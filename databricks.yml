workspace:
  host: https://dbc-dab2c37e-7a7b.cloud.databricks.com/
  profile: DEFAULT

bundle:
  name: montreal-15min-app

resources:
  apps:
    montreal_15min:
      name: montreal-15min-city-auditor
      source_code_path: .
      config:
        command:
          - uvicorn
          - app:app
          - --host
          - "0.0.0.0"
          - --port
          - "8000"
        env:
          - name: DATABRICKS_WAREHOUSE_ID
            value: "6cba794911e5618c"
          - name: LLM_ENDPOINT
            value: "databricks-llama-4-maverick"
