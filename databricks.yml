workspace:
  host: https://dbc-32d1558d-c1ca.cloud.databricks.com/
  profile: DEFAULT

bundle:
  name: montreal-15min-app

resources:
  apps:
    montreal_15min:
      name: montreal-15min-city-auditor
      source_code_path: .
      config:
        command:
          - uvicorn
          - app:app
          - --host
          - "0.0.0.0"
          - --port
          - "8000"
        env:
          - name: DATABRICKS_WAREHOUSE_ID
            value: "8d421519858864c7"
          - name: LLM_ENDPOINT
            value: "databricks-llama-4-maverick"
